const fs = require("fs"); let content = fs.readFileSync("app/landing/page.tsx", "utf8"); content = content.replace("export default function WindowCleaningAdsPage() {", "export default function WindowCleaningAdsPage() {\n    const [gmb, setGmb] = useState({ rating: \"5.0\", reviewCount: \"40+\" });\n    useEffect(() => { getGmbData().then(data => setGmb({ rating: data.rating.toFixed(1), reviewCount: data.reviewCount + \"+\" })); }, []);"); fs.writeFileSync("app/landing/page.tsx", content, "utf8");
