const fs = require("fs"); let content = fs.readFileSync("app/layout.tsx", "utf8"); content = content.replace("import { getGmbData } from \"@/app/actions/gmb\";", "import { getGmbData } from \"@/app/actions/gmb\";\nimport GmbProvider from \"@/components/GmbProvider\";"); content = content.replace(/<body className="antialiased min-h-screen flex flex-col">/, "<body className=\"antialiased min-h-screen flex flex-col\">\n                <GmbProvider value={{ rating: Number(gmbData.rating).toFixed(1), reviewCount: gmbData.reviewCount + \"+\", address: gmbData.address }}>"); content = content.replace(/<\/body>/, "                </GmbProvider>\n            </body>"); fs.writeFileSync("app/layout.tsx", content, "utf8");
